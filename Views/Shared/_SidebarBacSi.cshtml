<div class="d-flex flex-column flex-shrink-0 p-3 bg-light" style="width: 250px; min-height: 100vh;">
    <div class="mb-3">
        <span class="fs-5 fw-semibold text-primary">Bác sĩ</span>
    </div>

    <ul class="nav nav-pills flex-column mb-auto">
        
        <!-- Khám bệnh -->
        <li class="nav-item">
            <a class="nav-link @(ViewContext.RouteData.Values["Action"]?.ToString() == "Index" ? "active" : "")" 
               asp-controller="BacSi" asp-action="Index">
                <i class="bi bi-heart-pulse me-2"></i> Khám bệnh
            </a>
        </li>

        <!-- Danh sách bệnh nhân xét nghiệm -->
        <li class="nav-item">
            <a class="nav-link @(ViewContext.RouteData.Values["Action"]?.ToString() == "DanhSachXetNghiem" ? "active" : "")" 
               asp-controller="BacSi" asp-action="DanhSachXetNghiem">
                <i class="bi bi-clipboard-data me-2"></i> DS bệnh nhân xét nghiệm
            </a>
        </li>

    </ul>
</div>